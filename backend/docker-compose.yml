services:
  backend:
    build: .

    container_name: carshop-backend

    ports:
      - "5000:5000"

    env_file:
      - .env

    volumes:
      - .:/app
      - /app/node_modules

    command: npm run dev